# INSTAR next

v1.1.3 leftovers closed (skip links, Soil tone, LSB channel/bit extract, workbench/manual OG, Playwright Caesar NaN-key + LSB miss).

1. Done: skip-to-main on nymph, soil, brood, emerge, song, prime, liber, tunnel. Hello, 404, workbench, and the field manual already had them. Unit lab scans every HTML page for `a.skip` + `main#main`.
2. Done: workbench and field-manual Open Graph / Twitter tags. Same `og.jpg?v=1.1.3` card. Unique title, description, and URL. Unit lab plus Playwright check the share meta.
3. Done: Playwright `verify_lab.mjs` Caesar NaN-key (workbench encode with TIBERIVS stays HELLO) and LSB miss. Cheap 1-row wing PNG fixture in the lab (green bit 3 hits, red LSB misses). No payload file in `public/`.

Next low-risk: Playwright skip+main on remaining molt pages (soil, brood, emerge, prime, liber) if another cook wants browser cover. Unit lab already scans the HTML.

Do not paste plaintext answers into `public/`.
Do not commit `scripts/_secret_manifest.json` or `.orbit/`.
EN only. ASCII dashes.
