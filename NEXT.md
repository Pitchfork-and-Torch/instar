# INSTAR next

v1.2.0 Continue the molt is live. GitHub latest-only `v1.2.0`. Do not bump until a cut.

`INSTAR.resume()` is a real chamber path (never a hash). Complete molt hides Continue. Gate `#status` is a live region. Skip uses clip + :focus. Manual locked headings speak "locked". `unit_lab.js` boots `core.js`. Playwright covers continue from storage.

Next low-risk: only if another cook wants a new chamber or a field-manual pack. Do not invent a molt. Cut is a later tax.

Do not paste plaintext answers into `public/`.
Do not commit `scripts/_secret_manifest.json` or `.orbit/`.
EN only. ASCII dashes.
